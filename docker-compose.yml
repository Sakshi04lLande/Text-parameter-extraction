version: "3.9"

services:
  psychological-api:
    build: .
    container_name: psychological-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: always

    logging:
      options:
        max-size: "10m"
        max-file: "3"